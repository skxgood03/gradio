[tool.poetry]
name = "gradio-venv"
version = "0.1.0"
description = ""
authors = ["none"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
gradio = "^4.37.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
